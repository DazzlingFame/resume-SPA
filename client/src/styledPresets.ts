export const smallTextStyles = `
    font-family: Rubic, monospace;
    font-size: var(--small);
    line-height: var(--small-lineheight);
    user-select: none;
`;

export const boldSubheaderTextStyles = `
    font-family: Rubic-bold, monospace;
    font-size: var(--subHeader);
    user-select: none;
`;

export const regularText = `
    font-family: Rubic, monospace;
    font-size: var(--regular);
    user-select: none;
`;

